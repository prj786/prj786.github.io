// Every route is rendered to static HTML at build time — GitHub Pages then
// serves finished markup, no client-side rendering needed to read the site.
export const prerender = true;
export const trailingSlash = 'always';
